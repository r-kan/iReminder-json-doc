include templates/mixins/indexFolders

doctype html
head
  include templates/includes/css
  title iReminder JSON Documentation
html
  body.index
    article
      header
        h1 iReminder JSON Documentation

        p
          | Check each possible entries to specify, and find out
          | how iReminder reacts to them

      section
        h2 Documentation

        ul
          each file, name in files
            if typeof file === 'string'
              li
                a(href=name)= name

        each folder, name in files
          if typeof folder === 'object'
            h2= name
            ul
              mixin indexFolders(folder, 3)

      include templates/includes/footer
